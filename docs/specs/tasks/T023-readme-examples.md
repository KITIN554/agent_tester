# T023 — Усилить README + добавить examples/

## Приоритет: Желательно (#8, ~30 мин)

## Цель
README сейчас минимальный — для защиты ВКР и для постороннего читателя
нужно: «5 минут чтения → понятно что это и как запустить».

## Что нужно сделать

1. Расширить `README.md`:
   - **Что это** — одно-два абзаца с конкретной мотивацией главы 1 ВКР.
   - **Архитектурная диаграмма** (ASCII или ссылка на картинку).
   - **Quick start**: 5 команд (install → validate → run → дашборд → evolve).
   - **Структура проекта** — короткое описание каждого каталога.
   - **Метрики и пороги** — кратко 6 осей и формулы агрегации.
   - **Чему не место в этом репозитории** (синтетика данных, открытый код).
2. Создать `examples/` с двумя живыми артефактами:
   - `examples/run_finance_v1.html` — копия одного «хорошего» прогона
     finance (после T017).
   - `examples/run_travel_v1.html` — то же для travel.
   - `examples/scenario_finance.yaml` — один из лучших сценариев.
   - `examples/README.md` — что в каждом файле смотреть.
3. В корневой README ссылка: «Посмотри живой отчёт в `examples/run_finance_v1.html`».

## Acceptance criteria

- [ ] `README.md` содержит ≥6 секций (см. выше) и Quick start с 5 командами.
- [ ] `examples/` создан, ≥2 HTML-отчёта + 1 YAML + 1 README.
- [ ] `pytest` зелёный (тесты не зависят от README).
- [ ] Закоммичено: `docs: expand README and add examples/ directory`

## Зависимости
T017 (есть «хорошие» прогоны для копирования в examples).
